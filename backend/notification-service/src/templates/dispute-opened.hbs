<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Litige ouvert</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f5f5f5; margin: 0; padding: 0; }
    .container { max-width: 600px; margin: 40px auto; background: #fff; border-radius: 8px; overflow: hidden; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
    .header { background: #f59e0b; padding: 32px; text-align: center; }
    .header h1 { color: #fff; margin: 0; font-size: 28px; }
    .body { padding: 32px; color: #333; line-height: 1.6; }
    .receipt-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid #eee; font-size: 14px; }
    .btn { display: inline-block; margin: 24px 0; padding: 14px 32px; background: #6c3de0; color: #fff; text-decoration: none; border-radius: 6px; font-weight: bold; }
    .footer { padding: 20px 32px; background: #f9f9f9; color: #999; font-size: 12px; text-align: center; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header"><h1>⚠ Litige ouvert</h1></div>
    <div class="body">
      <p>Bonjour {{firstName}},</p>
      <p>Un acheteur a ouvert un litige concernant une commande de votre événement <strong>{{eventName}}</strong>.</p>
      <div class="receipt-row"><span>Référence commande</span><span><strong>{{orderReference}}</strong></span></div>
      <div class="receipt-row"><span>Événement</span><span>{{eventName}}</span></div>
      <div class="receipt-row"><span>Motif</span><span>{{reason}}</span></div>
      <p style="margin-top:24px;">Le reversement lié à cette commande est temporairement bloqué le temps que l'équipe BilletiX examine le litige. Vous serez notifié dès sa résolution.</p>
      <a href="{{dashboardUrl}}" class="btn">Voir mon tableau de bord</a>
    </div>
    <div class="footer">© {{year}} BilletiX — Tous droits réservés</div>
  </div>
</body>
</html>
